---
title: Getting Started
---
import { toast } from 'somoto'
import { Tabs, TabItem, Code } from '@astrojs/starlight/components';

This guide will help you get started with Somoto.
Sonner is good. So i think there should be a toast library for SolidJS.

### Install

<Tabs>
  <TabItem label="pnpm">
  ```bash 
  pnpm i somoto
  ```
  </TabItem>
  <TabItem label="npm">
  ```bash 
  npm i somoto
  ```
  </TabItem>
  <TabItem label="yarn">
  ```bash 
  yarn add somoto
  ```
  </TabItem>
  <TabItem label="bun">
  ```bash 
  bun add somoto
  ```
  </TabItem>
</Tabs>

### Add Toaster to your app

It can be placed anywhere, even in server components such as `layout.tsx`.

```tsx
import { Toaster } from 'somoto';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
```

### Render a toast

```tsx
import { toast } from 'somoto';

function MyToast() {
  return <button onClick={() => toast('This is a somoto toast')}>Render my toast</button>;
}
```
